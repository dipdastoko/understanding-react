import React from 'react';

const config = (props) => {
    return (
        <div>
            <ul>
                <li>Price: {props.price}</li>
            </ul>
        </div>
    );
};

export default config;